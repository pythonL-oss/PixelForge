# PixelForge
PixelForge converts any image into true low-res pixel art (16x16 to 128x128) with automatic subject extraction and transparent backgrounds. Detail-preserving SSIM downscaling, OKLab palette quantisation, one-click Minecraft item/block texture packs. Windows GUI + CLI.
